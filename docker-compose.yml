services:
  python_app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: app_python
    ports:
      - "${APP_PORT}:5000"
    env_file:
      - .env
    volumes:
      - .data:/app/data

    stdin_open: true
    tty: true
